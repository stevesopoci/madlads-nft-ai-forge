// styles/image.styles.ts
// Styles for the ImageScreen placeholder

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  // Layout
  container: {
    flex: 1,
    backgroundColor: "#000",
    justifyContent: "center",
    alignItems: "center",
  },

  // Typography
  text: {
    color: "#888",
    fontSize: 18,
  },
});

export default styles;
